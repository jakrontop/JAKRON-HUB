local util = {}

function util.distance(i,v)
   return (i.Position - v.Position).Magnitude
end

function util.teleport(obj,cf)
   obj.CFrame = cf
end

function util.getPlrNames()
 local p = game.Players:GetPlayers()
 local t = {}

 for i,v in pairs(p) do
    table.insert(t,p.Name)
 end

 return t
end

return util